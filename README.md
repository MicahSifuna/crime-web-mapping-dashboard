Get the data inside the client folder under public folder.
The data is in geojson format. You can convert to shapefile by using QGIS and then publish to PostgreSQL as we did. Then proceed with the pproject.
